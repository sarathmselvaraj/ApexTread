# ApexTread V6 QA

- Header theme/direction controls use two round reference-style icon buttons.
- Direction button keeps an accessible LTR/RTL state while using the bidirectional-arrow visual.
- Mobile navigation open/close and route-close behavior is verified at 360, 390, 414, 768 and 1024 widths.
- Pricing package cards use fixed content rows at tablet/desktop sizes so all three prices share one horizontal baseline.
- Existing ApexTread orange/neutral palette is unchanged.
